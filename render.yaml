services:
  - type: web
    name: riddle-phone
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npx ts-node src/index.ts
    envVars:
      - key: NODE_ENV
        value: production
